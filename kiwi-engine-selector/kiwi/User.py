
class User:

    """
    Either store user metainfo, or get it from user storage or get it via request?
    """

    def __init__(self, name, total):
        self.name = name
        self.total = total